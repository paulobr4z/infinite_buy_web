import { routes } from '../../../routes'

export const items = [
  { name: 'Dashboard', to: `${routes.dashboard}` },
  { name: 'Produtos', to: `${routes.products}` },
  { name: 'Usuários', to: `${routes.users}` },
  { name: 'Configurações', to: `${routes.setup}` },
]
